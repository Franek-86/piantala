import React from "react";
import BottomBar from "../components/BottomBar";
const Legend = () => {
  return (
    <>
      <section className='section-page'>Legend</section>
      <BottomBar />
    </>
  );
};

export default Legend;
